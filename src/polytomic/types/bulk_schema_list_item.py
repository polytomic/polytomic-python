# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class BulkSchemaListItem(UniversalBaseModel):
    data_cutoff_timestamp: typing.Optional[dt.datetime] = pydantic.Field(default=None)
    """
    Per-schema cutoff. Records older than this timestamp are excluded from sync runs.
    """

    disable_data_cutoff: typing.Optional[bool] = pydantic.Field(default=None)
    """
    When true, the sync ignores any configured data_cutoff_timestamp and syncs the full history of this schema.
    """

    enabled: typing.Optional[bool] = pydantic.Field(default=None)
    """
    Whether this schema is included in sync runs.
    """

    id: typing.Optional[str] = pydantic.Field(default=None)
    """
    Source-side schema identifier (e.g. object name for SaaS sources, schema.table for databases).
    """

    output_name: typing.Optional[str] = pydantic.Field(default=None)
    """
    Effective name of the object written to the destination after any configured naming rules are applied.
    """

    partition_key: typing.Optional[str] = pydantic.Field(default=None)
    """
    Source field used to partition rows when writing to the destination. Empty if the destination does not require one.
    """

    tracking_field: typing.Optional[str] = pydantic.Field(default=None)
    """
    Source field used to detect changes between incremental sync runs. Empty if the schema performs full resyncs.
    """

    user_output_name: typing.Optional[str] = pydantic.Field(default=None)
    """
    User-specified override for the destination object name. When empty, output_name is used.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
