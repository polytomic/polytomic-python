# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .target_filter_condition import TargetFilterCondition


class TargetFilters(UniversalBaseModel):
    """
    Filters applied to the destination records the sync has matched. Only some destinations and sync modes support them; 'supports_target_filters' on the mode returned by GET /api/connections/{id}/modelsync/target/fields says which.
    """

    conditions: typing.Optional[typing.List[TargetFilterCondition]] = pydantic.Field(default=None)
    """
    Conditions a destination record must satisfy to be written. An empty list writes every matched record.
    """

    logic: typing.Optional[str] = pydantic.Field(default=None)
    """
    Expression combining the conditions in this object by their labels; 'and', 'or', and parentheses are supported. The operators are not case-sensitive but the labels are, and the expression has to name every condition in this object -- a condition may be repeated, as in '(A and B) or (A and C)' -- and an expression which leaves one out is rejected. An empty expression means every condition must be satisfied.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
