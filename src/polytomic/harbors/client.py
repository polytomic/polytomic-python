# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.close_harbor_session_envelope import CloseHarborSessionEnvelope
from ..types.create_harbor_envelope import CreateHarborEnvelope
from ..types.deleted_harbor_context_draft_envelope import DeletedHarborContextDraftEnvelope
from ..types.deleted_harbor_context_envelope import DeletedHarborContextEnvelope
from ..types.deleted_harbor_envelope import DeletedHarborEnvelope
from ..types.harbor_connection_list_envelope import HarborConnectionListEnvelope
from ..types.harbor_connection_schema_envelope import HarborConnectionSchemaEnvelope
from ..types.harbor_context_draft_envelope import HarborContextDraftEnvelope
from ..types.harbor_context_draft_list_envelope import HarborContextDraftListEnvelope
from ..types.harbor_context_envelope import HarborContextEnvelope
from ..types.harbor_context_list_envelope import HarborContextListEnvelope
from ..types.harbor_context_version_envelope import HarborContextVersionEnvelope
from ..types.harbor_context_version_list_envelope import HarborContextVersionListEnvelope
from ..types.harbor_envelope import HarborEnvelope
from ..types.harbor_key_envelope import HarborKeyEnvelope
from ..types.harbor_key_list_envelope import HarborKeyListEnvelope
from ..types.harbor_list_envelope import HarborListEnvelope
from ..types.harbor_schema_list_envelope import HarborSchemaListEnvelope
from ..types.harbor_source_reference import HarborSourceReference
from ..types.harbor_status_envelope import HarborStatusEnvelope
from ..types.harbor_user_envelope import HarborUserEnvelope
from ..types.harbor_user_list_envelope import HarborUserListEnvelope
from ..types.register_harbor_session_envelope import RegisterHarborSessionEnvelope
from ..types.resolve_harbor_source_mappings_envelope import ResolveHarborSourceMappingsEnvelope
from ..types.revoked_harbor_key_envelope import RevokedHarborKeyEnvelope
from ..types.unassigned_harbor_user_envelope import UnassignedHarborUserEnvelope
from .raw_client import AsyncRawHarborsClient, RawHarborsClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class HarborsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawHarborsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawHarborsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawHarborsClient
        """
        return self._raw_client

    def list_authorized_connections(
        self,
        *,
        limit: typing.Optional[int] = None,
        page_token: typing.Optional[str] = None,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborConnectionListEnvelope:
        """
        Lists profile-authorized connections and capabilities for the current Harbor credential.

        Parameters
        ----------
        limit : typing.Optional[int]

        page_token : typing.Optional[str]

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborConnectionListEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.list_authorized_connections(
            limit=1,
            page_token="page_token",
        )
        """
        _response = self._raw_client.list_authorized_connections(
            limit=limit,
            page_token=page_token,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            request_options=request_options,
        )
        return _response.data

    def list_authorized_schemas(
        self,
        connection_id: str,
        *,
        limit: typing.Optional[int] = None,
        page_token: typing.Optional[str] = None,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborSchemaListEnvelope:
        """
        Lists one bounded page of schema resources authorized by the current Harbor profile.

        Parameters
        ----------
        connection_id : str

        limit : typing.Optional[int]

        page_token : typing.Optional[str]

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborSchemaListEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.list_authorized_schemas(
            connection_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            limit=1,
            page_token="page_token",
        )
        """
        _response = self._raw_client.list_authorized_schemas(
            connection_id,
            limit=limit,
            page_token=page_token,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            request_options=request_options,
        )
        return _response.data

    def get_authorized_schema(
        self,
        connection_id: str,
        schema_id: str,
        *,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborConnectionSchemaEnvelope:
        """
        Returns one schema resource authorized by the current Harbor profile.

        Parameters
        ----------
        connection_id : str

        schema_id : str

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborConnectionSchemaEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.get_authorized_schema(
            connection_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            schema_id="schema_id",
        )
        """
        _response = self._raw_client.get_authorized_schema(
            connection_id,
            schema_id,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            request_options=request_options,
        )
        return _response.data

    def register_session(
        self,
        *,
        polytomic_mcp_key_id: typing.Optional[str] = None,
        polytomic_mcp_timestamp: typing.Optional[int] = None,
        polytomic_mcp_nonce: typing.Optional[str] = None,
        polytomic_mcp_signature: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        client_name: typing.Optional[str] = OMIT,
        client_version: typing.Optional[str] = OMIT,
        external_run_id: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> RegisterHarborSessionEnvelope:
        """
        Registers a service-attested MCP transport session for a scoped Harbor credential.

        Parameters
        ----------
        polytomic_mcp_key_id : typing.Optional[str]

        polytomic_mcp_timestamp : typing.Optional[int]

        polytomic_mcp_nonce : typing.Optional[str]

        polytomic_mcp_signature : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        client_name : typing.Optional[str]
            MCP-observed client name.

        client_version : typing.Optional[str]
            MCP-observed client version.

        external_run_id : typing.Optional[str]
            Optional client-supplied run correlation value.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RegisterHarborSessionEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.register_session()
        """
        _response = self._raw_client.register_session(
            polytomic_mcp_key_id=polytomic_mcp_key_id,
            polytomic_mcp_timestamp=polytomic_mcp_timestamp,
            polytomic_mcp_nonce=polytomic_mcp_nonce,
            polytomic_mcp_signature=polytomic_mcp_signature,
            polytomic_activity_request_id=polytomic_activity_request_id,
            client_name=client_name,
            client_version=client_version,
            external_run_id=external_run_id,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )
        return _response.data

    def close_session(
        self,
        session_id: str,
        *,
        polytomic_mcp_key_id: typing.Optional[str] = None,
        polytomic_mcp_timestamp: typing.Optional[int] = None,
        polytomic_mcp_nonce: typing.Optional[str] = None,
        polytomic_mcp_signature: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        polytomic_harbor_session: typing.Optional[str] = None,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CloseHarborSessionEnvelope:
        """
        Closes a service-attested Harbor MCP transport session.

        Parameters
        ----------
        session_id : str

        polytomic_mcp_key_id : typing.Optional[str]

        polytomic_mcp_timestamp : typing.Optional[int]

        polytomic_mcp_nonce : typing.Optional[str]

        polytomic_mcp_signature : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        polytomic_harbor_session : typing.Optional[str]

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CloseHarborSessionEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.close_session(
            session_id="248df4b7-aa70-47b8-a036-33ac447e668d",
        )
        """
        _response = self._raw_client.close_session(
            session_id,
            polytomic_mcp_key_id=polytomic_mcp_key_id,
            polytomic_mcp_timestamp=polytomic_mcp_timestamp,
            polytomic_mcp_nonce=polytomic_mcp_nonce,
            polytomic_mcp_signature=polytomic_mcp_signature,
            polytomic_activity_request_id=polytomic_activity_request_id,
            polytomic_harbor_session=polytomic_harbor_session,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )
        return _response.data

    def list(
        self,
        *,
        limit: typing.Optional[int] = None,
        page_token: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborListEnvelope:
        """
        Lists Harbors in the caller's current organization.

        Returns Harbors in creation order. Use `pagination.next_page_token` to continue when more results are available.

        Parameters
        ----------
        limit : typing.Optional[int]
            Maximum number of Harbors to return. Defaults to 50 and cannot exceed 50.

        page_token : typing.Optional[str]
            Opaque pagination cursor returned by the previous request.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborListEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.list(
            limit=50,
            page_token="page_token",
        )
        """
        _response = self._raw_client.list(limit=limit, page_token=page_token, request_options=request_options)
        return _response.data

    def create(
        self,
        *,
        backing_mode: str,
        name: str,
        backing_connection_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        generate_api_key: typing.Optional[bool] = OMIT,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateHarborEnvelope:
        """
        Creates a managed or customer-managed Harbor in the caller's current organization.

        `generate_api_key` defaults to `true`. Polytomic returns a new plaintext credential only in this response. Set it to `false` to create the Harbor without a credential.

        For `customer_managed`, `backing_connection_id` must identify a queryable Connection that your credential can access.

        Parameters
        ----------
        backing_mode : str
            How the Harbor's queryable data store is provided. Valid values are managed and customer_managed.

        name : str
            Human-readable Harbor name.

        backing_connection_id : typing.Optional[str]
            Existing queryable Connection used by a customer-managed Harbor. Required only when backing_mode is customer_managed.

        description : typing.Optional[str]
            Short description of the Harbor. Maximum 1,000 characters.

        generate_api_key : typing.Optional[bool]
            Whether to generate a profile credential. Defaults to true.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateHarborEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.create(
            backing_mode="managed",
            name="Revenue Operations",
        )
        """
        _response = self._raw_client.create(
            backing_mode=backing_mode,
            name=name,
            backing_connection_id=backing_connection_id,
            description=description,
            generate_api_key=generate_api_key,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )
        return _response.data

    def get(self, harbor_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> HarborEnvelope:
        """
        Returns one Harbor by its first-class Harbor ID.

        The response exposes the backing Connection ID but not the internal profile used to authorize Harbor credentials.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.get(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
        )
        """
        _response = self._raw_client.get(harbor_id, request_options=request_options)
        return _response.data

    def update(
        self,
        harbor_id: str,
        *,
        name: str,
        description: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborEnvelope:
        """
        Updates a Harbor's name and description.

        Renaming a managed Harbor also renames its backing Connection. Both names
        change together; a name conflict returns `409 Conflict` and leaves both
        unchanged. Renaming a customer-managed Harbor preserves its Connection's name.

        This operation does not change `backing_mode` or `backing_connection_id`.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        name : str
            Human-readable Harbor name.

        description : typing.Optional[str]
            Short description of the Harbor. Maximum 1,000 characters.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.update(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            name="Revenue Operations",
        )
        """
        _response = self._raw_client.update(
            harbor_id,
            name=name,
            description=description,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )
        return _response.data

    def delete(
        self,
        harbor_id: str,
        *,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DeletedHarborEnvelope:
        """
        Deletes a Harbor and revokes its credentials.

        > 🚧 Harbor deletion
        >
        > Deleting a Harbor revokes its credentials, context documents, and user assignments. A customer-managed backing Connection remains available. Polytomic deletes a managed backing Connection only when no other resource uses it.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeletedHarborEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.delete(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
        )
        """
        _response = self._raw_client.delete(harbor_id, idempotency_key=idempotency_key, request_options=request_options)
        return _response.data

    def list_contexts(
        self,
        harbor_id: str,
        *,
        limit: typing.Optional[int] = None,
        page_token: typing.Optional[str] = None,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborContextListEnvelope:
        """
        Lists context document metadata for a Harbor without returning document content.

        Collection items include the current published `version` number and omit
        `content`. Use the context item endpoint to retrieve a complete document.

        A Harbor profile credential can read context only when `harbor_id` identifies
        its own Harbor.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        limit : typing.Optional[int]
            Maximum number of context documents to return. Defaults to 50 and cannot exceed 50.

        page_token : typing.Optional[str]
            Opaque pagination cursor returned by the previous request.

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborContextListEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.list_contexts(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            limit=50,
            page_token="page_token",
        )
        """
        _response = self._raw_client.list_contexts(
            harbor_id,
            limit=limit,
            page_token=page_token,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            request_options=request_options,
        )
        return _response.data

    def create_context(
        self,
        harbor_id: str,
        *,
        content: str,
        title: str,
        description: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborContextEnvelope:
        """
        Creates and attaches a context document to a Harbor.

        The new document belongs only to this Harbor. Context documents cannot be attached to multiple Harbors.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        content : str
            Plain-text context content. Maximum 20,000 characters.

        title : str
            Human-readable context title. Maximum 200 characters.

        description : typing.Optional[str]
            Short summary of the context document. Maximum 1,000 characters.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborContextEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.create_context(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            content="Bookings use the contract signed date...",
            title="Revenue definitions",
        )
        """
        _response = self._raw_client.create_context(
            harbor_id,
            content=content,
            title=title,
            description=description,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )
        return _response.data

    def list_context_drafts(
        self,
        harbor_id: str,
        *,
        limit: typing.Optional[int] = None,
        page_token: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborContextDraftListEnvelope:
        """
        Lists mutable Harbor context drafts without returning document content.

        The collection includes drafts for published context documents and initial drafts
        that have not yet been published. Use `context_id` with the draft detail,
        replacement, promotion, and discard endpoints.

        Draft metadata does not include `content`. Fetch a selected draft through its
        detail endpoint to read the complete candidate. Normal context list and detail
        operations continue to return published content only.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        limit : typing.Optional[int]
            Maximum number of mutable drafts to return. Defaults to 50 and cannot exceed 50.

        page_token : typing.Optional[str]
            Opaque pagination cursor returned by the previous request.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborContextDraftListEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.list_context_drafts(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            limit=50,
            page_token="page_token",
        )
        """
        _response = self._raw_client.list_context_drafts(
            harbor_id, limit=limit, page_token=page_token, request_options=request_options
        )
        return _response.data

    def create_context_draft(
        self,
        harbor_id: str,
        *,
        content: str,
        title: str,
        change_note: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborContextDraftEnvelope:
        """
        Creates an unpublished context document with its initial mutable draft.

        Creates a stable context identity and its initial mutable draft without publishing
        content to the Harbor. The response includes `context_id`, which identifies the
        draft replacement, promotion, and discard routes.

        The initial draft has a null `base_revision_id`. It remains absent from normal
        context list/detail, GraphQL, entity lookup, and Harbor MCP reads until promoted.
        Use the regular context creation endpoint instead when the initial payload should
        be published immediately.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        content : str
            Complete plain-text draft content. Maximum 20,000 characters.

        title : str
            Human-readable draft title. Maximum 200 characters.

        change_note : typing.Optional[str]
            Optional note stored with the draft and copied to published version 1 on promotion.

        description : typing.Optional[str]
            Short summary of the draft. Maximum 1,000 characters.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborContextDraftEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.create_context_draft(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            content="Bookings use the contract signed date...",
            title="Revenue definitions",
        )
        """
        _response = self._raw_client.create_context_draft(
            harbor_id,
            content=content,
            title=title,
            change_note=change_note,
            description=description,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )
        return _response.data

    def get_context(
        self,
        harbor_id: str,
        context_id: str,
        *,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborContextEnvelope:
        """
        Returns one complete Harbor context document.

        The response includes the current published `version` number and complete
        plain-text `content`.

        A Harbor profile credential can read context only when `harbor_id` identifies
        its own Harbor.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        context_id : str
            Unique identifier of the context document.

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborContextEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.get_context(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            context_id="248df4b7-aa70-47b8-a036-33ac447e668d",
        )
        """
        _response = self._raw_client.get_context(
            harbor_id,
            context_id,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            request_options=request_options,
        )
        return _response.data

    def update_context(
        self,
        harbor_id: str,
        context_id: str,
        *,
        content: str,
        title: str,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        change_note: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborContextEnvelope:
        """
        Replaces one Harbor context document.

        Each successful request publishes the next immutable version. You can omit
        `change_note`.

        A direct publication leaves an existing draft unchanged.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        context_id : str
            Unique identifier of the context document.

        content : str
            Plain-text context content. Maximum 20,000 characters.

        title : str
            Human-readable context title. Maximum 200 characters.

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        change_note : typing.Optional[str]
            Optional note describing this published change.

        description : typing.Optional[str]
            Short summary of the context document. Maximum 1,000 characters.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborContextEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.update_context(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            context_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            content="Bookings use the contract signed date...",
            title="Revenue definitions",
        )
        """
        _response = self._raw_client.update_context(
            harbor_id,
            context_id,
            content=content,
            title=title,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            change_note=change_note,
            description=description,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )
        return _response.data

    def delete_context(
        self,
        harbor_id: str,
        context_id: str,
        *,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DeletedHarborContextEnvelope:
        """
        Deletes one context document from a Harbor.

        Deleting a context document does not affect the Harbor or its other context documents.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        context_id : str
            Unique identifier of the context document.

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeletedHarborContextEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.delete_context(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            context_id="248df4b7-aa70-47b8-a036-33ac447e668d",
        )
        """
        _response = self._raw_client.delete_context(
            harbor_id,
            context_id,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )
        return _response.data

    def get_context_draft(
        self,
        harbor_id: str,
        context_id: str,
        *,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborContextDraftEnvelope:
        """
        Returns the mutable draft for a Harbor context document.

        Drafts are available only through the draft endpoints. `base_revision_id`
        identifies the published revision from which the candidate was created. Normal
        context reads and Harbor MCP tools continue to return the current published
        version. Creator and updater IDs are null when their actor type is `system`.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        context_id : str
            Unique identifier of the context document.

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborContextDraftEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.get_context_draft(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            context_id="248df4b7-aa70-47b8-a036-33ac447e668d",
        )
        """
        _response = self._raw_client.get_context_draft(
            harbor_id,
            context_id,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            request_options=request_options,
        )
        return _response.data

    def save_context_draft(
        self,
        harbor_id: str,
        context_id: str,
        *,
        content: str,
        title: str,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        change_note: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborContextDraftEnvelope:
        """
        Creates or completely replaces the mutable draft for a Harbor context document.

        The request supplies the complete draft payload. If a draft already exists,
        this request replaces it while preserving the draft ID, creation metadata, and
        `base_revision_id`.

        A new draft for a published document records the current revision as its base.
        An unpublished document's initial draft keeps a null base when replaced. Saving
        a draft does not change published content or its `updated_at`. Creator and
        updater provenance comes from the request actor; system actors have a null actor
        ID.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        context_id : str
            Unique identifier of the context document.

        content : str
            Complete plain-text draft content. Maximum 20,000 characters.

        title : str
            Human-readable draft title. Maximum 200 characters.

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        change_note : typing.Optional[str]
            Optional note stored with the draft and copied to the published version on promotion.

        description : typing.Optional[str]
            Short summary of the draft. Maximum 1,000 characters.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborContextDraftEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.save_context_draft(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            context_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            content="Bookings use the contract signed date...",
            title="Revenue definitions",
        )
        """
        _response = self._raw_client.save_context_draft(
            harbor_id,
            context_id,
            content=content,
            title=title,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            change_note=change_note,
            description=description,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )
        return _response.data

    def delete_context_draft(
        self,
        harbor_id: str,
        context_id: str,
        *,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DeletedHarborContextDraftEnvelope:
        """
        Discards the mutable draft for a Harbor context document.

        Discarding a draft does not change the current published version or its
        history. If the draft belongs to a context that has never been published,
        discarding it also removes the otherwise empty context identity.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        context_id : str
            Unique identifier of the context document.

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeletedHarborContextDraftEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.delete_context_draft(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            context_id="248df4b7-aa70-47b8-a036-33ac447e668d",
        )
        """
        _response = self._raw_client.delete_context_draft(
            harbor_id,
            context_id,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )
        return _response.data

    def promote_context_draft(
        self,
        harbor_id: str,
        context_id: str,
        *,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborContextVersionEnvelope:
        """
        Promotes the draft to the next immutable published context version.

        Promotion publishes the draft's exact title, description, content, and optional
        change note as the next version. An unpublished context's initial draft becomes
        version 1. The draft is removed after publication.

        Promotion returns a conflict when the current published revision differs from
        the draft's `base_revision_id`. The stale draft remains available so an author
        can compare it with the current version before discarding and recreating it.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        context_id : str
            Unique identifier of the context document.

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborContextVersionEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.promote_context_draft(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            context_id="248df4b7-aa70-47b8-a036-33ac447e668d",
        )
        """
        _response = self._raw_client.promote_context_draft(
            harbor_id,
            context_id,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )
        return _response.data

    def list_context_versions(
        self,
        harbor_id: str,
        context_id: str,
        *,
        limit: typing.Optional[int] = None,
        page_token: typing.Optional[str] = None,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborContextVersionListEnvelope:
        """
        Lists immutable published versions of a Harbor context document without returning content.

        Versions are ordered from newest to oldest. Collection items omit `content`,
        and the draft is never included. Pagination tokens continue from the last
        returned version, so publishing a newer version between requests does not shift
        or duplicate older results.

        Published revision IDs are durable artifact identities intended for future
        Harbor activity and audit records. Publisher IDs are paired with actor types.
        System publications have `published_by_type: "system"` and a null
        `published_by` because the system actor has no UUID.

        A Harbor profile credential can read versions only when `harbor_id` identifies
        its own Harbor.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        context_id : str
            Unique identifier of the context document.

        limit : typing.Optional[int]
            Maximum number of published versions to return. Defaults to 50 and cannot exceed 50.

        page_token : typing.Optional[str]
            Opaque pagination cursor returned by the previous request.

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborContextVersionListEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.list_context_versions(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            context_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            limit=50,
            page_token="page_token",
        )
        """
        _response = self._raw_client.list_context_versions(
            harbor_id,
            context_id,
            limit=limit,
            page_token=page_token,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            request_options=request_options,
        )
        return _response.data

    def get_context_version(
        self,
        harbor_id: str,
        context_id: str,
        version_id: str,
        *,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborContextVersionEnvelope:
        """
        Returns one immutable published version of a Harbor context document.

        The response includes the complete title, description, and plain-text
        `content` captured when the version was published.

        Published versions cannot be changed or deleted. System publications have
        `published_by_type: "system"` and a null `published_by`.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        context_id : str
            Unique identifier of the context document.

        version_id : str
            Unique identifier of the published context version.

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborContextVersionEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.get_context_version(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            context_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            version_id="248df4b7-aa70-47b8-a036-33ac447e668d",
        )
        """
        _response = self._raw_client.get_context_version(
            harbor_id,
            context_id,
            version_id,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            request_options=request_options,
        )
        return _response.data

    def list_keys(
        self,
        harbor_id: str,
        *,
        limit: typing.Optional[int] = None,
        page_token: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborKeyListEnvelope:
        """
        Lists active masked credentials for a Harbor.

        Each item contains a masked `key_hint`. Polytomic never returns a credential plaintext after creation.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        limit : typing.Optional[int]
            Maximum number of active credentials to return. Defaults to 50 and cannot exceed 50.

        page_token : typing.Optional[str]
            Opaque pagination cursor returned by the previous request.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborKeyListEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.list_keys(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            limit=50,
            page_token="page_token",
        )
        """
        _response = self._raw_client.list_keys(
            harbor_id, limit=limit, page_token=page_token, request_options=request_options
        )
        return _response.data

    def create_key(
        self,
        harbor_id: str,
        *,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborKeyEnvelope:
        """
        Generates a new Harbor credential and returns its plaintext value once.

        Store the returned `value` securely. Polytomic returns it only in this response.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborKeyEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.create_key(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
        )
        """
        _response = self._raw_client.create_key(
            harbor_id, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def delete_key(
        self,
        harbor_id: str,
        key_id: str,
        *,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> RevokedHarborKeyEnvelope:
        """
        Revokes one Harbor credential by its credential ID.

        Revocation affects only the selected credential. Other active Harbor credentials remain valid.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        key_id : str
            Unique identifier of the Harbor credential.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RevokedHarborKeyEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.delete_key(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            key_id="248df4b7-aa70-47b8-a036-33ac447e668d",
        )
        """
        _response = self._raw_client.delete_key(
            harbor_id, key_id, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def resolve_source_mappings(
        self,
        harbor_id: str,
        *,
        sources: typing.Optional[typing.Sequence[HarborSourceReference]] = OMIT,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ResolveHarborSourceMappingsEnvelope:
        """
        Resolves documented source table and field identities to the names a Harbor's backing Connection accepts.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        sources : typing.Optional[typing.Sequence[HarborSourceReference]]
            Source tables to resolve. Answered in request order; maximum 200 entries, each naming at most 500 fields.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ResolveHarborSourceMappingsEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.resolve_source_mappings(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
        )
        """
        _response = self._raw_client.resolve_source_mappings(
            harbor_id, sources=sources, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def get_status(
        self, harbor_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> HarborStatusEnvelope:
        """
        Returns raw Polytomic refresh evidence for datasets written to a Harbor.

        Each pipeline corresponds to a bulk sync or model sync that writes at least one
        dataset to the Harbor's backing Connection. Tables populated outside Polytomic
        are not included, even when they are queryable through a customer-managed
        backing Connection.

        The response groups shared pipeline evidence so schedules and configuration are
        not repeated for every dataset:

        - Each entry in `pipelines` identifies the producer through `type` and `id`.
          Separate pipelines targeting the same physical dataset remain separate
          entries.
        - `datasets` is keyed by the effective destination dataset name.
          `last_success_at` is the start time of the most recent execution in which that
          dataset completed successfully, including a successful dataset within a bulk
          execution that completes with errors. The start time is a conservative upper
          bound because source reads and destination writes happen afterward.
        - `latest_status` preserves the latest Polytomic execution status. Never-run
          datasets omit this field.
        - Pipeline-level `schedules` preserves schedule parameters and selectors. A
          schedule can be manual, event-driven, advanced, selective, or limited to
          named source schemas, so callers should not reduce the list to one inferred
          cadence.
        - Continuous schedules use the scheduler's persisted next firing. If scheduler
          state is unavailable, `next_run_at` is omitted rather than recalculated with
          new jitter.
        - Paused pipelines remain present with `refresh_enabled` set to `false` and no
          `next_run_at`.

        Use absolute timestamps and the raw statuses to apply the maximum acceptable
        staleness for your task. The endpoint does not classify datasets or the Harbor
        as healthy, stale, or unhealthy.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborStatusEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.get_status(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
        )
        """
        _response = self._raw_client.get_status(harbor_id, request_options=request_options)
        return _response.data

    def list_users(
        self,
        harbor_id: str,
        *,
        limit: typing.Optional[int] = None,
        page_token: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborUserListEnvelope:
        """
        Lists Harbor-only users assigned to a Harbor.

        The response contains only Harbor-only users currently assigned to this Harbor.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        limit : typing.Optional[int]
            Maximum number of assigned users to return. Defaults to 50 and cannot exceed 50.

        page_token : typing.Optional[str]
            Opaque pagination cursor returned by the previous request.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborUserListEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.list_users(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            limit=50,
            page_token="page_token",
        )
        """
        _response = self._raw_client.list_users(
            harbor_id, limit=limit, page_token=page_token, request_options=request_options
        )
        return _response.data

    def invite_user(
        self,
        harbor_id: str,
        *,
        email: str,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborUserEnvelope:
        """
        Invites and assigns a new Harbor-only user.

        The invited account is restricted to assigned Harbors and does not receive regular Polytomic application access.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        email : str
            Email address used to invite the Harbor-only user.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborUserEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.invite_user(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            email="analyst@example.com",
        )
        """
        _response = self._raw_client.invite_user(
            harbor_id, email=email, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def assign_user(
        self,
        harbor_id: str,
        user_id: str,
        *,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborUserEnvelope:
        """
        Assigns an existing Harbor-only user to a Harbor.

        The assignment is idempotent. Regular Polytomic users cannot be assigned because they already have application access to Harbors.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        user_id : str
            Unique identifier of the Harbor-only user.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborUserEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.assign_user(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            user_id="248df4b7-aa70-47b8-a036-33ac447e668d",
        )
        """
        _response = self._raw_client.assign_user(
            harbor_id, user_id, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def unassign_user(
        self,
        harbor_id: str,
        user_id: str,
        *,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UnassignedHarborUserEnvelope:
        """
        Removes a Harbor assignment without deleting the user.

        This removes only the Harbor assignment. The organization user remains available and may retain assignments to other Harbors.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        user_id : str
            Unique identifier of the Harbor-only user.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UnassignedHarborUserEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.harbors.unassign_user(
            harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            user_id="248df4b7-aa70-47b8-a036-33ac447e668d",
        )
        """
        _response = self._raw_client.unassign_user(
            harbor_id, user_id, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data


class AsyncHarborsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawHarborsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawHarborsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawHarborsClient
        """
        return self._raw_client

    async def list_authorized_connections(
        self,
        *,
        limit: typing.Optional[int] = None,
        page_token: typing.Optional[str] = None,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborConnectionListEnvelope:
        """
        Lists profile-authorized connections and capabilities for the current Harbor credential.

        Parameters
        ----------
        limit : typing.Optional[int]

        page_token : typing.Optional[str]

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborConnectionListEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.list_authorized_connections(
                limit=1,
                page_token="page_token",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_authorized_connections(
            limit=limit,
            page_token=page_token,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            request_options=request_options,
        )
        return _response.data

    async def list_authorized_schemas(
        self,
        connection_id: str,
        *,
        limit: typing.Optional[int] = None,
        page_token: typing.Optional[str] = None,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborSchemaListEnvelope:
        """
        Lists one bounded page of schema resources authorized by the current Harbor profile.

        Parameters
        ----------
        connection_id : str

        limit : typing.Optional[int]

        page_token : typing.Optional[str]

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborSchemaListEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.list_authorized_schemas(
                connection_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                limit=1,
                page_token="page_token",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_authorized_schemas(
            connection_id,
            limit=limit,
            page_token=page_token,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            request_options=request_options,
        )
        return _response.data

    async def get_authorized_schema(
        self,
        connection_id: str,
        schema_id: str,
        *,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborConnectionSchemaEnvelope:
        """
        Returns one schema resource authorized by the current Harbor profile.

        Parameters
        ----------
        connection_id : str

        schema_id : str

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborConnectionSchemaEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.get_authorized_schema(
                connection_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                schema_id="schema_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_authorized_schema(
            connection_id,
            schema_id,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            request_options=request_options,
        )
        return _response.data

    async def register_session(
        self,
        *,
        polytomic_mcp_key_id: typing.Optional[str] = None,
        polytomic_mcp_timestamp: typing.Optional[int] = None,
        polytomic_mcp_nonce: typing.Optional[str] = None,
        polytomic_mcp_signature: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        client_name: typing.Optional[str] = OMIT,
        client_version: typing.Optional[str] = OMIT,
        external_run_id: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> RegisterHarborSessionEnvelope:
        """
        Registers a service-attested MCP transport session for a scoped Harbor credential.

        Parameters
        ----------
        polytomic_mcp_key_id : typing.Optional[str]

        polytomic_mcp_timestamp : typing.Optional[int]

        polytomic_mcp_nonce : typing.Optional[str]

        polytomic_mcp_signature : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        client_name : typing.Optional[str]
            MCP-observed client name.

        client_version : typing.Optional[str]
            MCP-observed client version.

        external_run_id : typing.Optional[str]
            Optional client-supplied run correlation value.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RegisterHarborSessionEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.register_session()


        asyncio.run(main())
        """
        _response = await self._raw_client.register_session(
            polytomic_mcp_key_id=polytomic_mcp_key_id,
            polytomic_mcp_timestamp=polytomic_mcp_timestamp,
            polytomic_mcp_nonce=polytomic_mcp_nonce,
            polytomic_mcp_signature=polytomic_mcp_signature,
            polytomic_activity_request_id=polytomic_activity_request_id,
            client_name=client_name,
            client_version=client_version,
            external_run_id=external_run_id,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )
        return _response.data

    async def close_session(
        self,
        session_id: str,
        *,
        polytomic_mcp_key_id: typing.Optional[str] = None,
        polytomic_mcp_timestamp: typing.Optional[int] = None,
        polytomic_mcp_nonce: typing.Optional[str] = None,
        polytomic_mcp_signature: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        polytomic_harbor_session: typing.Optional[str] = None,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CloseHarborSessionEnvelope:
        """
        Closes a service-attested Harbor MCP transport session.

        Parameters
        ----------
        session_id : str

        polytomic_mcp_key_id : typing.Optional[str]

        polytomic_mcp_timestamp : typing.Optional[int]

        polytomic_mcp_nonce : typing.Optional[str]

        polytomic_mcp_signature : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        polytomic_harbor_session : typing.Optional[str]

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CloseHarborSessionEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.close_session(
                session_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.close_session(
            session_id,
            polytomic_mcp_key_id=polytomic_mcp_key_id,
            polytomic_mcp_timestamp=polytomic_mcp_timestamp,
            polytomic_mcp_nonce=polytomic_mcp_nonce,
            polytomic_mcp_signature=polytomic_mcp_signature,
            polytomic_activity_request_id=polytomic_activity_request_id,
            polytomic_harbor_session=polytomic_harbor_session,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )
        return _response.data

    async def list(
        self,
        *,
        limit: typing.Optional[int] = None,
        page_token: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborListEnvelope:
        """
        Lists Harbors in the caller's current organization.

        Returns Harbors in creation order. Use `pagination.next_page_token` to continue when more results are available.

        Parameters
        ----------
        limit : typing.Optional[int]
            Maximum number of Harbors to return. Defaults to 50 and cannot exceed 50.

        page_token : typing.Optional[str]
            Opaque pagination cursor returned by the previous request.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborListEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.list(
                limit=50,
                page_token="page_token",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list(limit=limit, page_token=page_token, request_options=request_options)
        return _response.data

    async def create(
        self,
        *,
        backing_mode: str,
        name: str,
        backing_connection_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        generate_api_key: typing.Optional[bool] = OMIT,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateHarborEnvelope:
        """
        Creates a managed or customer-managed Harbor in the caller's current organization.

        `generate_api_key` defaults to `true`. Polytomic returns a new plaintext credential only in this response. Set it to `false` to create the Harbor without a credential.

        For `customer_managed`, `backing_connection_id` must identify a queryable Connection that your credential can access.

        Parameters
        ----------
        backing_mode : str
            How the Harbor's queryable data store is provided. Valid values are managed and customer_managed.

        name : str
            Human-readable Harbor name.

        backing_connection_id : typing.Optional[str]
            Existing queryable Connection used by a customer-managed Harbor. Required only when backing_mode is customer_managed.

        description : typing.Optional[str]
            Short description of the Harbor. Maximum 1,000 characters.

        generate_api_key : typing.Optional[bool]
            Whether to generate a profile credential. Defaults to true.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateHarborEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.create(
                backing_mode="managed",
                name="Revenue Operations",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            backing_mode=backing_mode,
            name=name,
            backing_connection_id=backing_connection_id,
            description=description,
            generate_api_key=generate_api_key,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )
        return _response.data

    async def get(self, harbor_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> HarborEnvelope:
        """
        Returns one Harbor by its first-class Harbor ID.

        The response exposes the backing Connection ID but not the internal profile used to authorize Harbor credentials.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.get(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(harbor_id, request_options=request_options)
        return _response.data

    async def update(
        self,
        harbor_id: str,
        *,
        name: str,
        description: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborEnvelope:
        """
        Updates a Harbor's name and description.

        Renaming a managed Harbor also renames its backing Connection. Both names
        change together; a name conflict returns `409 Conflict` and leaves both
        unchanged. Renaming a customer-managed Harbor preserves its Connection's name.

        This operation does not change `backing_mode` or `backing_connection_id`.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        name : str
            Human-readable Harbor name.

        description : typing.Optional[str]
            Short description of the Harbor. Maximum 1,000 characters.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.update(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                name="Revenue Operations",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            harbor_id,
            name=name,
            description=description,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )
        return _response.data

    async def delete(
        self,
        harbor_id: str,
        *,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DeletedHarborEnvelope:
        """
        Deletes a Harbor and revokes its credentials.

        > 🚧 Harbor deletion
        >
        > Deleting a Harbor revokes its credentials, context documents, and user assignments. A customer-managed backing Connection remains available. Polytomic deletes a managed backing Connection only when no other resource uses it.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeletedHarborEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.delete(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(
            harbor_id, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def list_contexts(
        self,
        harbor_id: str,
        *,
        limit: typing.Optional[int] = None,
        page_token: typing.Optional[str] = None,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborContextListEnvelope:
        """
        Lists context document metadata for a Harbor without returning document content.

        Collection items include the current published `version` number and omit
        `content`. Use the context item endpoint to retrieve a complete document.

        A Harbor profile credential can read context only when `harbor_id` identifies
        its own Harbor.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        limit : typing.Optional[int]
            Maximum number of context documents to return. Defaults to 50 and cannot exceed 50.

        page_token : typing.Optional[str]
            Opaque pagination cursor returned by the previous request.

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborContextListEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.list_contexts(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                limit=50,
                page_token="page_token",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_contexts(
            harbor_id,
            limit=limit,
            page_token=page_token,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            request_options=request_options,
        )
        return _response.data

    async def create_context(
        self,
        harbor_id: str,
        *,
        content: str,
        title: str,
        description: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborContextEnvelope:
        """
        Creates and attaches a context document to a Harbor.

        The new document belongs only to this Harbor. Context documents cannot be attached to multiple Harbors.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        content : str
            Plain-text context content. Maximum 20,000 characters.

        title : str
            Human-readable context title. Maximum 200 characters.

        description : typing.Optional[str]
            Short summary of the context document. Maximum 1,000 characters.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborContextEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.create_context(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                content="Bookings use the contract signed date...",
                title="Revenue definitions",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_context(
            harbor_id,
            content=content,
            title=title,
            description=description,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )
        return _response.data

    async def list_context_drafts(
        self,
        harbor_id: str,
        *,
        limit: typing.Optional[int] = None,
        page_token: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborContextDraftListEnvelope:
        """
        Lists mutable Harbor context drafts without returning document content.

        The collection includes drafts for published context documents and initial drafts
        that have not yet been published. Use `context_id` with the draft detail,
        replacement, promotion, and discard endpoints.

        Draft metadata does not include `content`. Fetch a selected draft through its
        detail endpoint to read the complete candidate. Normal context list and detail
        operations continue to return published content only.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        limit : typing.Optional[int]
            Maximum number of mutable drafts to return. Defaults to 50 and cannot exceed 50.

        page_token : typing.Optional[str]
            Opaque pagination cursor returned by the previous request.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborContextDraftListEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.list_context_drafts(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                limit=50,
                page_token="page_token",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_context_drafts(
            harbor_id, limit=limit, page_token=page_token, request_options=request_options
        )
        return _response.data

    async def create_context_draft(
        self,
        harbor_id: str,
        *,
        content: str,
        title: str,
        change_note: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborContextDraftEnvelope:
        """
        Creates an unpublished context document with its initial mutable draft.

        Creates a stable context identity and its initial mutable draft without publishing
        content to the Harbor. The response includes `context_id`, which identifies the
        draft replacement, promotion, and discard routes.

        The initial draft has a null `base_revision_id`. It remains absent from normal
        context list/detail, GraphQL, entity lookup, and Harbor MCP reads until promoted.
        Use the regular context creation endpoint instead when the initial payload should
        be published immediately.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        content : str
            Complete plain-text draft content. Maximum 20,000 characters.

        title : str
            Human-readable draft title. Maximum 200 characters.

        change_note : typing.Optional[str]
            Optional note stored with the draft and copied to published version 1 on promotion.

        description : typing.Optional[str]
            Short summary of the draft. Maximum 1,000 characters.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborContextDraftEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.create_context_draft(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                content="Bookings use the contract signed date...",
                title="Revenue definitions",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_context_draft(
            harbor_id,
            content=content,
            title=title,
            change_note=change_note,
            description=description,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )
        return _response.data

    async def get_context(
        self,
        harbor_id: str,
        context_id: str,
        *,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborContextEnvelope:
        """
        Returns one complete Harbor context document.

        The response includes the current published `version` number and complete
        plain-text `content`.

        A Harbor profile credential can read context only when `harbor_id` identifies
        its own Harbor.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        context_id : str
            Unique identifier of the context document.

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborContextEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.get_context(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                context_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_context(
            harbor_id,
            context_id,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            request_options=request_options,
        )
        return _response.data

    async def update_context(
        self,
        harbor_id: str,
        context_id: str,
        *,
        content: str,
        title: str,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        change_note: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborContextEnvelope:
        """
        Replaces one Harbor context document.

        Each successful request publishes the next immutable version. You can omit
        `change_note`.

        A direct publication leaves an existing draft unchanged.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        context_id : str
            Unique identifier of the context document.

        content : str
            Plain-text context content. Maximum 20,000 characters.

        title : str
            Human-readable context title. Maximum 200 characters.

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        change_note : typing.Optional[str]
            Optional note describing this published change.

        description : typing.Optional[str]
            Short summary of the context document. Maximum 1,000 characters.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborContextEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.update_context(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                context_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                content="Bookings use the contract signed date...",
                title="Revenue definitions",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_context(
            harbor_id,
            context_id,
            content=content,
            title=title,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            change_note=change_note,
            description=description,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )
        return _response.data

    async def delete_context(
        self,
        harbor_id: str,
        context_id: str,
        *,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DeletedHarborContextEnvelope:
        """
        Deletes one context document from a Harbor.

        Deleting a context document does not affect the Harbor or its other context documents.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        context_id : str
            Unique identifier of the context document.

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeletedHarborContextEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.delete_context(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                context_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_context(
            harbor_id,
            context_id,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )
        return _response.data

    async def get_context_draft(
        self,
        harbor_id: str,
        context_id: str,
        *,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborContextDraftEnvelope:
        """
        Returns the mutable draft for a Harbor context document.

        Drafts are available only through the draft endpoints. `base_revision_id`
        identifies the published revision from which the candidate was created. Normal
        context reads and Harbor MCP tools continue to return the current published
        version. Creator and updater IDs are null when their actor type is `system`.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        context_id : str
            Unique identifier of the context document.

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborContextDraftEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.get_context_draft(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                context_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_context_draft(
            harbor_id,
            context_id,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            request_options=request_options,
        )
        return _response.data

    async def save_context_draft(
        self,
        harbor_id: str,
        context_id: str,
        *,
        content: str,
        title: str,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        change_note: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborContextDraftEnvelope:
        """
        Creates or completely replaces the mutable draft for a Harbor context document.

        The request supplies the complete draft payload. If a draft already exists,
        this request replaces it while preserving the draft ID, creation metadata, and
        `base_revision_id`.

        A new draft for a published document records the current revision as its base.
        An unpublished document's initial draft keeps a null base when replaced. Saving
        a draft does not change published content or its `updated_at`. Creator and
        updater provenance comes from the request actor; system actors have a null actor
        ID.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        context_id : str
            Unique identifier of the context document.

        content : str
            Complete plain-text draft content. Maximum 20,000 characters.

        title : str
            Human-readable draft title. Maximum 200 characters.

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        change_note : typing.Optional[str]
            Optional note stored with the draft and copied to the published version on promotion.

        description : typing.Optional[str]
            Short summary of the draft. Maximum 1,000 characters.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborContextDraftEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.save_context_draft(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                context_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                content="Bookings use the contract signed date...",
                title="Revenue definitions",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.save_context_draft(
            harbor_id,
            context_id,
            content=content,
            title=title,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            change_note=change_note,
            description=description,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )
        return _response.data

    async def delete_context_draft(
        self,
        harbor_id: str,
        context_id: str,
        *,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DeletedHarborContextDraftEnvelope:
        """
        Discards the mutable draft for a Harbor context document.

        Discarding a draft does not change the current published version or its
        history. If the draft belongs to a context that has never been published,
        discarding it also removes the otherwise empty context identity.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        context_id : str
            Unique identifier of the context document.

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeletedHarborContextDraftEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.delete_context_draft(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                context_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_context_draft(
            harbor_id,
            context_id,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )
        return _response.data

    async def promote_context_draft(
        self,
        harbor_id: str,
        context_id: str,
        *,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborContextVersionEnvelope:
        """
        Promotes the draft to the next immutable published context version.

        Promotion publishes the draft's exact title, description, content, and optional
        change note as the next version. An unpublished context's initial draft becomes
        version 1. The draft is removed after publication.

        Promotion returns a conflict when the current published revision differs from
        the draft's `base_revision_id`. The stale draft remains available so an author
        can compare it with the current version before discarding and recreating it.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        context_id : str
            Unique identifier of the context document.

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborContextVersionEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.promote_context_draft(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                context_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.promote_context_draft(
            harbor_id,
            context_id,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            idempotency_key=idempotency_key,
            request_options=request_options,
        )
        return _response.data

    async def list_context_versions(
        self,
        harbor_id: str,
        context_id: str,
        *,
        limit: typing.Optional[int] = None,
        page_token: typing.Optional[str] = None,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborContextVersionListEnvelope:
        """
        Lists immutable published versions of a Harbor context document without returning content.

        Versions are ordered from newest to oldest. Collection items omit `content`,
        and the draft is never included. Pagination tokens continue from the last
        returned version, so publishing a newer version between requests does not shift
        or duplicate older results.

        Published revision IDs are durable artifact identities intended for future
        Harbor activity and audit records. Publisher IDs are paired with actor types.
        System publications have `published_by_type: "system"` and a null
        `published_by` because the system actor has no UUID.

        A Harbor profile credential can read versions only when `harbor_id` identifies
        its own Harbor.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        context_id : str
            Unique identifier of the context document.

        limit : typing.Optional[int]
            Maximum number of published versions to return. Defaults to 50 and cannot exceed 50.

        page_token : typing.Optional[str]
            Opaque pagination cursor returned by the previous request.

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborContextVersionListEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.list_context_versions(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                context_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                limit=50,
                page_token="page_token",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_context_versions(
            harbor_id,
            context_id,
            limit=limit,
            page_token=page_token,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            request_options=request_options,
        )
        return _response.data

    async def get_context_version(
        self,
        harbor_id: str,
        context_id: str,
        version_id: str,
        *,
        polytomic_harbor_session: typing.Optional[str] = None,
        polytomic_activity_request_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborContextVersionEnvelope:
        """
        Returns one immutable published version of a Harbor context document.

        The response includes the complete title, description, and plain-text
        `content` captured when the version was published.

        Published versions cannot be changed or deleted. System publications have
        `published_by_type: "system"` and a null `published_by`.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        context_id : str
            Unique identifier of the context document.

        version_id : str
            Unique identifier of the published context version.

        polytomic_harbor_session : typing.Optional[str]

        polytomic_activity_request_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborContextVersionEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.get_context_version(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                context_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                version_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_context_version(
            harbor_id,
            context_id,
            version_id,
            polytomic_harbor_session=polytomic_harbor_session,
            polytomic_activity_request_id=polytomic_activity_request_id,
            request_options=request_options,
        )
        return _response.data

    async def list_keys(
        self,
        harbor_id: str,
        *,
        limit: typing.Optional[int] = None,
        page_token: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborKeyListEnvelope:
        """
        Lists active masked credentials for a Harbor.

        Each item contains a masked `key_hint`. Polytomic never returns a credential plaintext after creation.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        limit : typing.Optional[int]
            Maximum number of active credentials to return. Defaults to 50 and cannot exceed 50.

        page_token : typing.Optional[str]
            Opaque pagination cursor returned by the previous request.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborKeyListEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.list_keys(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                limit=50,
                page_token="page_token",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_keys(
            harbor_id, limit=limit, page_token=page_token, request_options=request_options
        )
        return _response.data

    async def create_key(
        self,
        harbor_id: str,
        *,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborKeyEnvelope:
        """
        Generates a new Harbor credential and returns its plaintext value once.

        Store the returned `value` securely. Polytomic returns it only in this response.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborKeyEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.create_key(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_key(
            harbor_id, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def delete_key(
        self,
        harbor_id: str,
        key_id: str,
        *,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> RevokedHarborKeyEnvelope:
        """
        Revokes one Harbor credential by its credential ID.

        Revocation affects only the selected credential. Other active Harbor credentials remain valid.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        key_id : str
            Unique identifier of the Harbor credential.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RevokedHarborKeyEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.delete_key(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                key_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_key(
            harbor_id, key_id, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def resolve_source_mappings(
        self,
        harbor_id: str,
        *,
        sources: typing.Optional[typing.Sequence[HarborSourceReference]] = OMIT,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ResolveHarborSourceMappingsEnvelope:
        """
        Resolves documented source table and field identities to the names a Harbor's backing Connection accepts.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        sources : typing.Optional[typing.Sequence[HarborSourceReference]]
            Source tables to resolve. Answered in request order; maximum 200 entries, each naming at most 500 fields.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ResolveHarborSourceMappingsEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.resolve_source_mappings(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.resolve_source_mappings(
            harbor_id, sources=sources, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def get_status(
        self, harbor_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> HarborStatusEnvelope:
        """
        Returns raw Polytomic refresh evidence for datasets written to a Harbor.

        Each pipeline corresponds to a bulk sync or model sync that writes at least one
        dataset to the Harbor's backing Connection. Tables populated outside Polytomic
        are not included, even when they are queryable through a customer-managed
        backing Connection.

        The response groups shared pipeline evidence so schedules and configuration are
        not repeated for every dataset:

        - Each entry in `pipelines` identifies the producer through `type` and `id`.
          Separate pipelines targeting the same physical dataset remain separate
          entries.
        - `datasets` is keyed by the effective destination dataset name.
          `last_success_at` is the start time of the most recent execution in which that
          dataset completed successfully, including a successful dataset within a bulk
          execution that completes with errors. The start time is a conservative upper
          bound because source reads and destination writes happen afterward.
        - `latest_status` preserves the latest Polytomic execution status. Never-run
          datasets omit this field.
        - Pipeline-level `schedules` preserves schedule parameters and selectors. A
          schedule can be manual, event-driven, advanced, selective, or limited to
          named source schemas, so callers should not reduce the list to one inferred
          cadence.
        - Continuous schedules use the scheduler's persisted next firing. If scheduler
          state is unavailable, `next_run_at` is omitted rather than recalculated with
          new jitter.
        - Paused pipelines remain present with `refresh_enabled` set to `false` and no
          `next_run_at`.

        Use absolute timestamps and the raw statuses to apply the maximum acceptable
        staleness for your task. The endpoint does not classify datasets or the Harbor
        as healthy, stale, or unhealthy.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborStatusEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.get_status(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_status(harbor_id, request_options=request_options)
        return _response.data

    async def list_users(
        self,
        harbor_id: str,
        *,
        limit: typing.Optional[int] = None,
        page_token: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborUserListEnvelope:
        """
        Lists Harbor-only users assigned to a Harbor.

        The response contains only Harbor-only users currently assigned to this Harbor.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        limit : typing.Optional[int]
            Maximum number of assigned users to return. Defaults to 50 and cannot exceed 50.

        page_token : typing.Optional[str]
            Opaque pagination cursor returned by the previous request.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborUserListEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.list_users(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                limit=50,
                page_token="page_token",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_users(
            harbor_id, limit=limit, page_token=page_token, request_options=request_options
        )
        return _response.data

    async def invite_user(
        self,
        harbor_id: str,
        *,
        email: str,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborUserEnvelope:
        """
        Invites and assigns a new Harbor-only user.

        The invited account is restricted to assigned Harbors and does not receive regular Polytomic application access.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        email : str
            Email address used to invite the Harbor-only user.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborUserEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.invite_user(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                email="analyst@example.com",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.invite_user(
            harbor_id, email=email, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def assign_user(
        self,
        harbor_id: str,
        user_id: str,
        *,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborUserEnvelope:
        """
        Assigns an existing Harbor-only user to a Harbor.

        The assignment is idempotent. Regular Polytomic users cannot be assigned because they already have application access to Harbors.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        user_id : str
            Unique identifier of the Harbor-only user.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborUserEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.assign_user(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                user_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.assign_user(
            harbor_id, user_id, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def unassign_user(
        self,
        harbor_id: str,
        user_id: str,
        *,
        idempotency_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UnassignedHarborUserEnvelope:
        """
        Removes a Harbor assignment without deleting the user.

        This removes only the Harbor assignment. The organization user remains available and may retain assignments to other Harbors.

        Parameters
        ----------
        harbor_id : str
            Unique identifier of the Harbor.

        user_id : str
            Unique identifier of the Harbor-only user.

        idempotency_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UnassignedHarborUserEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.harbors.unassign_user(
                harbor_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                user_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.unassign_user(
            harbor_id, user_id, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data
