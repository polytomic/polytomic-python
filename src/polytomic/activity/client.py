# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.harbor_activity_event_envelope import HarborActivityEventEnvelope
from ..types.harbor_activity_list_envelope import HarborActivityListEnvelope
from ..types.harbor_activity_session_envelope import HarborActivitySessionEnvelope
from .raw_client import AsyncRawActivityClient, RawActivityClient
from .types.activity_get_session_request_order import ActivityGetSessionRequestOrder
from .types.activity_list_request_order import ActivityListRequestOrder


class ActivityClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawActivityClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawActivityClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawActivityClient
        """
        return self._raw_client

    def list(
        self,
        *,
        start_time: dt.datetime,
        end_time: dt.datetime,
        limit: typing.Optional[int] = None,
        page_token: typing.Optional[str] = None,
        order: typing.Optional[ActivityListRequestOrder] = None,
        harbor_id: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        event_type: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        outcome: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        operation_id: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        session_id: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        actor_id: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        credential_id: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        connection_id: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborActivityListEnvelope:
        """
        Lists Harbor Activity recorded for the caller's organization.

        You must be an active Organization administrator to use this endpoint.

        `start_time` is inclusive and `end_time` is exclusive. Both filter the time at
        which Polytomic recorded each event, which can differ from `occurred_at`. A
        single request can cover at most 90 days.

        Results are ordered from newest to oldest by default. Set `order` to `asc` to
        retrieve the oldest events first. A page contains at most 100 events. Pass
        `next_page_token` from the response as `page_token` with the same time range,
        filters, order, and limit to retrieve the next page. The token expires 24 hours
        after the first page. Pagination uses a live descending cursor, so an event from
        a transaction that commits between pages can require a fresh traversal.

        Responses include stable identities, bounded resource snapshots, schema label
        snapshots with truncation state, field counts, and row counts when available.
        They do not include internal recorded-order values, submitted SQL, raw field
        names, detailed provider errors, result rows, or raw pagination tokens.

        > ℹ️ Activity retention
        >
        > Polytomic retains Activity events for at least 90 days. Pagination tokens
        > expire 24 hours after the first page.

        Parameters
        ----------
        start_time : dt.datetime
            Inclusive start of the event recorded-time range.

        end_time : dt.datetime
            Exclusive end of the event recorded-time range. The range can span at most 90 days.

        limit : typing.Optional[int]
            Maximum number of events to return. Defaults to 50 and cannot exceed 100.

        page_token : typing.Optional[str]
            Opaque pagination cursor returned by the previous request. It expires 24 hours after the first page.

        order : typing.Optional[ActivityListRequestOrder]
            Order in which events were recorded. Defaults to desc.

        harbor_id : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Return events for these Harbors. Repeat the parameter to filter by multiple IDs.

        event_type : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Return events with these event types. Repeat the parameter to filter by multiple types.

        outcome : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Return events with these outcomes. Repeat the parameter to filter by multiple outcomes.

        operation_id : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Return events for these operation IDs.

        session_id : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Return events for these session IDs.

        actor_id : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Return events for these actor IDs.

        credential_id : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Return events for these credential IDs.

        connection_id : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Return events for these Connection IDs.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborActivityListEnvelope
            OK

        Examples
        --------
        import datetime

        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.activity.list(
            start_time=datetime.datetime.fromisoformat(
                "2024-01-15 09:30:00+00:00",
            ),
            end_time=datetime.datetime.fromisoformat(
                "2024-01-15 09:30:00+00:00",
            ),
            limit=1,
            page_token="page_token",
            order="asc",
            harbor_id=["248df4b7-aa70-47b8-a036-33ac447e668d"],
            event_type=["event_type"],
            outcome=["outcome"],
            operation_id=["248df4b7-aa70-47b8-a036-33ac447e668d"],
            session_id=["248df4b7-aa70-47b8-a036-33ac447e668d"],
            actor_id=["248df4b7-aa70-47b8-a036-33ac447e668d"],
            credential_id=["248df4b7-aa70-47b8-a036-33ac447e668d"],
            connection_id=["248df4b7-aa70-47b8-a036-33ac447e668d"],
        )
        """
        _response = self._raw_client.list(
            start_time=start_time,
            end_time=end_time,
            limit=limit,
            page_token=page_token,
            order=order,
            harbor_id=harbor_id,
            event_type=event_type,
            outcome=outcome,
            operation_id=operation_id,
            session_id=session_id,
            actor_id=actor_id,
            credential_id=credential_id,
            connection_id=connection_id,
            request_options=request_options,
        )
        return _response.data

    def get_session(
        self,
        session_id: str,
        *,
        limit: typing.Optional[int] = None,
        page_token: typing.Optional[str] = None,
        order: typing.Optional[ActivityGetSessionRequestOrder] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborActivitySessionEnvelope:
        """
        Returns one Harbor Activity session and its events.

        You must be an active Organization administrator to use this endpoint. The
        session must belong to the Organization against which you authenticated.

        Events are ordered from oldest to newest by default. Set `order` to `desc` to
        retrieve the newest events first. A page contains at most 100 events. Pass
        `next_page_token` from the response as `page_token` with the same order and
        limit to retrieve the next page.

        `history_truncated` is `true` when Activity retention removed events from the
        beginning of the session. The lifecycle summary remains available while the
        session itself is retained.

        The response uses the same event allowlist as the Activity list and detail
        endpoints. It does not include session tokens, submitted SQL, raw field names,
        detailed provider errors, result rows, or raw pagination tokens.

        > ℹ️ Activity retention
        >
        > Polytomic retains Activity events for at least 90 days. Pagination tokens
        > expire 24 hours after the first page.

        Parameters
        ----------
        session_id : str
            Harbor Activity session ID.

        limit : typing.Optional[int]
            Maximum number of events to return. Defaults to 50 and cannot exceed 100.

        page_token : typing.Optional[str]
            Opaque pagination cursor returned by the previous request. It expires 24 hours after the first page.

        order : typing.Optional[ActivityGetSessionRequestOrder]
            Order in which events were recorded. Defaults to asc.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborActivitySessionEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.activity.get_session(
            session_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            limit=1,
            page_token="page_token",
            order="asc",
        )
        """
        _response = self._raw_client.get_session(
            session_id, limit=limit, page_token=page_token, order=order, request_options=request_options
        )
        return _response.data

    def get(
        self, event_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> HarborActivityEventEnvelope:
        """
        Returns one Harbor Activity event from the caller's organization.

        You must be an active Organization administrator to use this endpoint. The
        event must belong to the Organization against which you authenticated.

        The response can include stable identities, bounded resource snapshots, schema
        label snapshots with truncation state, field counts, and row counts. It does
        not include internal recorded-order values, submitted SQL, raw field names,
        detailed provider errors, result rows, or raw pagination tokens.

        > ℹ️ Activity retention
        >
        > Polytomic retains Activity events for at least 90 days.

        Parameters
        ----------
        event_id : str
            Activity event ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborActivityEventEnvelope
            OK

        Examples
        --------
        from polytomic import Polytomic

        client = Polytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )
        client.activity.get(
            event_id="248df4b7-aa70-47b8-a036-33ac447e668d",
        )
        """
        _response = self._raw_client.get(event_id, request_options=request_options)
        return _response.data


class AsyncActivityClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawActivityClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawActivityClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawActivityClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        start_time: dt.datetime,
        end_time: dt.datetime,
        limit: typing.Optional[int] = None,
        page_token: typing.Optional[str] = None,
        order: typing.Optional[ActivityListRequestOrder] = None,
        harbor_id: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        event_type: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        outcome: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        operation_id: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        session_id: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        actor_id: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        credential_id: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        connection_id: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborActivityListEnvelope:
        """
        Lists Harbor Activity recorded for the caller's organization.

        You must be an active Organization administrator to use this endpoint.

        `start_time` is inclusive and `end_time` is exclusive. Both filter the time at
        which Polytomic recorded each event, which can differ from `occurred_at`. A
        single request can cover at most 90 days.

        Results are ordered from newest to oldest by default. Set `order` to `asc` to
        retrieve the oldest events first. A page contains at most 100 events. Pass
        `next_page_token` from the response as `page_token` with the same time range,
        filters, order, and limit to retrieve the next page. The token expires 24 hours
        after the first page. Pagination uses a live descending cursor, so an event from
        a transaction that commits between pages can require a fresh traversal.

        Responses include stable identities, bounded resource snapshots, schema label
        snapshots with truncation state, field counts, and row counts when available.
        They do not include internal recorded-order values, submitted SQL, raw field
        names, detailed provider errors, result rows, or raw pagination tokens.

        > ℹ️ Activity retention
        >
        > Polytomic retains Activity events for at least 90 days. Pagination tokens
        > expire 24 hours after the first page.

        Parameters
        ----------
        start_time : dt.datetime
            Inclusive start of the event recorded-time range.

        end_time : dt.datetime
            Exclusive end of the event recorded-time range. The range can span at most 90 days.

        limit : typing.Optional[int]
            Maximum number of events to return. Defaults to 50 and cannot exceed 100.

        page_token : typing.Optional[str]
            Opaque pagination cursor returned by the previous request. It expires 24 hours after the first page.

        order : typing.Optional[ActivityListRequestOrder]
            Order in which events were recorded. Defaults to desc.

        harbor_id : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Return events for these Harbors. Repeat the parameter to filter by multiple IDs.

        event_type : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Return events with these event types. Repeat the parameter to filter by multiple types.

        outcome : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Return events with these outcomes. Repeat the parameter to filter by multiple outcomes.

        operation_id : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Return events for these operation IDs.

        session_id : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Return events for these session IDs.

        actor_id : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Return events for these actor IDs.

        credential_id : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Return events for these credential IDs.

        connection_id : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Return events for these Connection IDs.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborActivityListEnvelope
            OK

        Examples
        --------
        import asyncio
        import datetime

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.activity.list(
                start_time=datetime.datetime.fromisoformat(
                    "2024-01-15 09:30:00+00:00",
                ),
                end_time=datetime.datetime.fromisoformat(
                    "2024-01-15 09:30:00+00:00",
                ),
                limit=1,
                page_token="page_token",
                order="asc",
                harbor_id=["248df4b7-aa70-47b8-a036-33ac447e668d"],
                event_type=["event_type"],
                outcome=["outcome"],
                operation_id=["248df4b7-aa70-47b8-a036-33ac447e668d"],
                session_id=["248df4b7-aa70-47b8-a036-33ac447e668d"],
                actor_id=["248df4b7-aa70-47b8-a036-33ac447e668d"],
                credential_id=["248df4b7-aa70-47b8-a036-33ac447e668d"],
                connection_id=["248df4b7-aa70-47b8-a036-33ac447e668d"],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list(
            start_time=start_time,
            end_time=end_time,
            limit=limit,
            page_token=page_token,
            order=order,
            harbor_id=harbor_id,
            event_type=event_type,
            outcome=outcome,
            operation_id=operation_id,
            session_id=session_id,
            actor_id=actor_id,
            credential_id=credential_id,
            connection_id=connection_id,
            request_options=request_options,
        )
        return _response.data

    async def get_session(
        self,
        session_id: str,
        *,
        limit: typing.Optional[int] = None,
        page_token: typing.Optional[str] = None,
        order: typing.Optional[ActivityGetSessionRequestOrder] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HarborActivitySessionEnvelope:
        """
        Returns one Harbor Activity session and its events.

        You must be an active Organization administrator to use this endpoint. The
        session must belong to the Organization against which you authenticated.

        Events are ordered from oldest to newest by default. Set `order` to `desc` to
        retrieve the newest events first. A page contains at most 100 events. Pass
        `next_page_token` from the response as `page_token` with the same order and
        limit to retrieve the next page.

        `history_truncated` is `true` when Activity retention removed events from the
        beginning of the session. The lifecycle summary remains available while the
        session itself is retained.

        The response uses the same event allowlist as the Activity list and detail
        endpoints. It does not include session tokens, submitted SQL, raw field names,
        detailed provider errors, result rows, or raw pagination tokens.

        > ℹ️ Activity retention
        >
        > Polytomic retains Activity events for at least 90 days. Pagination tokens
        > expire 24 hours after the first page.

        Parameters
        ----------
        session_id : str
            Harbor Activity session ID.

        limit : typing.Optional[int]
            Maximum number of events to return. Defaults to 50 and cannot exceed 100.

        page_token : typing.Optional[str]
            Opaque pagination cursor returned by the previous request. It expires 24 hours after the first page.

        order : typing.Optional[ActivityGetSessionRequestOrder]
            Order in which events were recorded. Defaults to asc.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborActivitySessionEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.activity.get_session(
                session_id="248df4b7-aa70-47b8-a036-33ac447e668d",
                limit=1,
                page_token="page_token",
                order="asc",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_session(
            session_id, limit=limit, page_token=page_token, order=order, request_options=request_options
        )
        return _response.data

    async def get(
        self, event_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> HarborActivityEventEnvelope:
        """
        Returns one Harbor Activity event from the caller's organization.

        You must be an active Organization administrator to use this endpoint. The
        event must belong to the Organization against which you authenticated.

        The response can include stable identities, bounded resource snapshots, schema
        label snapshots with truncation state, field counts, and row counts. It does
        not include internal recorded-order values, submitted SQL, raw field names,
        detailed provider errors, result rows, or raw pagination tokens.

        > ℹ️ Activity retention
        >
        > Polytomic retains Activity events for at least 90 days.

        Parameters
        ----------
        event_id : str
            Activity event ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HarborActivityEventEnvelope
            OK

        Examples
        --------
        import asyncio

        from polytomic import AsyncPolytomic

        client = AsyncPolytomic(
            "2025-09-18",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.activity.get(
                event_id="248df4b7-aa70-47b8-a036-33ac447e668d",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(event_id, request_options=request_options)
        return _response.data
